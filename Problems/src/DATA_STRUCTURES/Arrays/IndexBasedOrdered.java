package DATA_STRUCTURES.Arrays;

/*
Array a = [A, C, D, B]
Array b = [0, 2, 3, 1]

Result c = [A, B, C, D]
*/


public class IndexBasedOrdered {

    public static char[] orderArray(int[] intArr, char[] charArr) {
        if (intArr.length < 2) {
            return charArr;
        }

        for (int i = 0; i < intArr.length; i++) {

        }

        return charArr;
    }


}
